package io.github.kaktushose.jdac.annotations.interactions;

/// Enum describing the two possible scopes a command can be registered for.
///
/// @see CommandConfig#scope()
public enum CommandScope {
    GUILD,
    GLOBAL
}
