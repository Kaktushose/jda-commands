invalid-implementation = Cannot provide implementation { $class } by annotating class with @Implementation.
multiple-instances =
    Multiple instances of { $type } found, that are registered via @Implementation.
    Only one can be provided! Found instances: { $found }